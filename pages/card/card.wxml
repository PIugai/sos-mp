<!--pages/card/card.wxml-->


<view>{{post.capacity}}</view>
<view>{{post.location}}</view>
<view wx:for="{{post.comments}}">
  <view>{{item.comment}}</view>
</view>
<button type="default" bindtap="bindOnTouchStart">Navigate</button>